---
title: Using Preact
description: Easily use Bamboo with Preact with our dedicated integration.
---

<Callout type="warning">
  This guide sets Bamboo up through PostCSS, which emits the stylesheet without compiling source: `css()` and `cva()`
  stay runtime calls and the generated style engine ships in your client bundle. Because Preact builds with Vite,
  [`@bamboocss/vite`](/docs/installation/vite) is the better default — it compiles those calls to literal class strings
  and ships no engine at all. Bamboo says the same thing at build time when it finds a Vite config.
</Callout>

## Start a new project

<Steps>

### Create Vite project

To get started, we will need to create a new Preact project using the `preact-ts` template.

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  {/* <!-- prettier-ignore-start --> */}
  <Tab>
    ```bash
    pnpm create vite test-app --template preact-ts
    cd test-app
    pnpm install
    ```
  </Tab>
  <Tab>
    ```bash
    npm create vite@latest test-app -- --template preact-ts
    cd test-app
    npm install
    ```
  </Tab>
  <Tab>
    ```bash
    yarn create vite test-app --template preact-ts
    cd test-app
    yarn
    ```
  </Tab>
  <Tab>
    ```bash
    bun create vite test-app --template preact-ts
    cd test-app
    bun install
    ```
  </Tab>
  {/* <!-- prettier-ignore-end --> */}
</Tabs>

### Install Bamboo

Install bamboo and create your `bamboo.config.ts` file.

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  {/* <!-- prettier-ignore-start --> */}
  <Tab>
    ```bash
    pnpm install -D @bamboocss/dev
    pnpm bamboo init --postcss
    ```
  </Tab>
  <Tab>
    ```bash
    npm install -D @bamboocss/dev
    npx bamboo init --postcss
    ```
  </Tab>
  <Tab>
    ```bash
    yarn add -D @bamboocss/dev
    yarn bamboo init --postcss
    ```
  </Tab>
  <Tab>
    ```bash
    bun add -D @bamboocss/dev
    bun bamboo init --postcss
    ```
  </Tab>
  {/* <!-- prettier-ignore-end --> */}
</Tabs>

### Update package.json scripts

Add `"prepare": "bamboo codegen"` to `scripts`. It runs [codegen](/docs/references/cli#codegen) after every dependency
install, so the output directory can stay in `.gitignore`.

### Configure the content

Make sure that all of the paths of your Preact components are included in the `include` section of the
`bamboo.config.ts` file.

```js {6} filename="bamboo.config.ts"
import { defineConfig } from '@bamboocss/dev'

export default defineConfig({
  preflight: true,
  // Where to look for your css declarations
  include: ['./src/**/*.{js,jsx,ts,tsx}', './pages/**/*.{js,jsx,ts,tsx}'],
  exclude: [],
  outdir: 'styled-system',
})
```

### Configure the entry CSS with layers

Add this code to the `src/index.css` file that `src/main.tsx` already imports.

```css filename="src/index.css"
@layer reset, base, tokens, recipes, utilities;
```

## Start your build process

Start the dev server with `pnpm dev`.

### Start using Bamboo

```tsx filename="src/app.tsx"
import { css } from '../styled-system/css'

export const App = () => {
  return <div class={css({ fontSize: '2xl', fontWeight: 'bold' })}>Hello 🎋!</div>
}
```

</Steps>

## Troubleshooting

If your IDE does not autocomplete `styled-system` imports, add that directory to `include` in `tsconfig.json`. See
[the CLI guide](/docs/installation/cli#troubleshooting).
